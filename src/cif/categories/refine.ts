import { Category, Schema } from './';

export const Refine_Schema = {
    'aniso_B[1][1]': Schema.float,
    'aniso_B[1][2]': Schema.float,
    'aniso_B[1][3]': Schema.float,
    'aniso_B[2][2]': Schema.float,
    'aniso_B[2][3]': Schema.float,
    'aniso_B[3][3]': Schema.float,
    B_iso_max: Schema.float,
    B_iso_mean: Schema.float,
    B_iso_min: Schema.float,
    correlation_coeff_Fo_to_Fc: Schema.float,
    correlation_coeff_Fo_to_Fc_free: Schema.float,
    details: Schema.str,
    diff_density_max: Schema.float,
    diff_density_max_esd: Schema.float,
    diff_density_min: Schema.float,
    diff_density_min_esd: Schema.float,
    diff_density_rms: Schema.float,
    diff_density_rms_esd: Schema.float,
    entry_id: Schema.strM,
    pdbx_refine_id: Schema.strM,
    ls_abs_structure_details: Schema.str,
    ls_abs_structure_Flack: Schema.float,
    ls_abs_structure_Flack_esd: Schema.float,
    ls_abs_structure_Rogers: Schema.float,
    ls_abs_structure_Rogers_esd: Schema.float,
    ls_d_res_high: Schema.floatM,
    ls_d_res_low: Schema.float,
    ls_extinction_coef: Schema.float,
    ls_extinction_coef_esd: Schema.float,
    ls_extinction_expression: Schema.str,
    ls_extinction_method: Schema.str,
    ls_goodness_of_fit_all: Schema.float,
    ls_goodness_of_fit_all_esd: Schema.float,
    ls_goodness_of_fit_obs: Schema.float,
    ls_goodness_of_fit_obs_esd: Schema.float,
    ls_hydrogen_treatment: Schema.Enum<'refall'|'refxyz'|'refU'|'noref'|'constr'|'mixed'|'undef'>(['refall', 'refxyz', 'refU', 'noref', 'constr', 'mixed', 'undef']),
    ls_matrix_type: Schema.Enum<'full'|'fullcycle'|'atomblock'|'userblock'|'diagonal'|'sparse'>(['full', 'fullcycle', 'atomblock', 'userblock', 'diagonal', 'sparse']),
    ls_number_constraints: Schema.int,
    ls_number_parameters: Schema.int,
    ls_number_reflns_all: Schema.int,
    ls_number_reflns_obs: Schema.int,
    ls_number_reflns_R_free: Schema.int,
    ls_number_reflns_R_work: Schema.int,
    ls_number_restraints: Schema.int,
    ls_percent_reflns_obs: Schema.float,
    ls_percent_reflns_R_free: Schema.float,
    ls_R_factor_all: Schema.float,
    ls_R_factor_obs: Schema.float,
    ls_R_factor_R_free: Schema.float,
    ls_R_factor_R_free_error: Schema.float,
    ls_R_factor_R_free_error_details: Schema.str,
    ls_R_factor_R_work: Schema.float,
    ls_R_Fsqd_factor_obs: Schema.float,
    ls_R_I_factor_obs: Schema.float,
    ls_redundancy_reflns_all: Schema.float,
    ls_redundancy_reflns_obs: Schema.float,
    ls_restrained_S_all: Schema.float,
    ls_restrained_S_obs: Schema.float,
    ls_shift_over_esd_max: Schema.float,
    ls_shift_over_esd_mean: Schema.float,
    ls_structure_factor_coef: Schema.Enum<'F'|'Fsqd'|'Inet'>(['F', 'Fsqd', 'Inet']),
    ls_weighting_details: Schema.str,
    ls_weighting_scheme: Schema.Enum<'sigma'|'unit'|'calc'>(['sigma', 'unit', 'calc']),
    ls_wR_factor_all: Schema.float,
    ls_wR_factor_obs: Schema.float,
    ls_wR_factor_R_free: Schema.float,
    ls_wR_factor_R_work: Schema.float,
    occupancy_max: Schema.float,
    occupancy_min: Schema.float,
    solvent_model_details: Schema.str,
    solvent_model_param_bsol: Schema.float,
    solvent_model_param_ksol: Schema.float,
    pdbx_R_complete: Schema.float,
    ls_R_factor_gt: Schema.float,
    ls_goodness_of_fit_gt: Schema.float,
    ls_goodness_of_fit_ref: Schema.float,
    ls_shift_over_su_max: Schema.float,
    ls_shift_over_su_max_lt: Schema.float,
    ls_shift_over_su_mean: Schema.float,
    ls_shift_over_su_mean_lt: Schema.float,
    pdbx_ls_sigma_I: Schema.float,
    pdbx_ls_sigma_F: Schema.float,
    pdbx_ls_sigma_Fsqd: Schema.float,
    pdbx_data_cutoff_high_absF: Schema.float,
    pdbx_data_cutoff_high_rms_absF: Schema.float,
    pdbx_data_cutoff_low_absF: Schema.float,
    pdbx_isotropic_thermal_model: Schema.str,
    pdbx_ls_cross_valid_method: Schema.str,
    pdbx_method_to_determine_struct: Schema.str,
    pdbx_starting_model: Schema.str,
    pdbx_stereochemistry_target_values: Schema.str,
    pdbx_R_Free_selection_details: Schema.str,
    pdbx_stereochem_target_val_spec_case: Schema.str,
    pdbx_overall_ESU_R: Schema.float,
    pdbx_overall_ESU_R_Free: Schema.float,
    pdbx_solvent_vdw_probe_radii: Schema.float,
    pdbx_solvent_ion_probe_radii: Schema.float,
    pdbx_solvent_shrinkage_radii: Schema.float,
    pdbx_real_space_R: Schema.float,
    pdbx_density_correlation: Schema.float,
    pdbx_pd_number_of_powder_patterns: Schema.int,
    pdbx_pd_number_of_points: Schema.int,
    pdbx_pd_meas_number_of_points: Schema.int,
    pdbx_pd_proc_ls_prof_R_factor: Schema.float,
    pdbx_pd_proc_ls_prof_wR_factor: Schema.float,
    pdbx_pd_Marquardt_correlation_coeff: Schema.float,
    pdbx_pd_Fsqrd_R_factor: Schema.float,
    pdbx_pd_ls_matrix_band_width: Schema.int,
    pdbx_overall_phase_error: Schema.float,
    pdbx_overall_SU_R_free_Cruickshank_DPI: Schema.float,
    pdbx_overall_SU_R_free_Blow_DPI: Schema.float,
    pdbx_overall_SU_R_Blow_DPI: Schema.float,
    pdbx_TLS_residual_ADP_flag: Schema.Enum<'LIKELY RESIDUAL'|'UNVERIFIED'>(['LIKELY RESIDUAL', 'UNVERIFIED']),
    pdbx_diffrn_id: Schema.str,
    overall_SU_B: Schema.float,
    overall_SU_ML: Schema.float,
    overall_SU_R_Cruickshank_DPI: Schema.float,
    overall_SU_R_free: Schema.float,
    overall_FOM_free_R_set: Schema.float,
    overall_FOM_work_R_set: Schema.float,
    pdbx_average_fsc_overall: Schema.float,
    pdbx_average_fsc_work: Schema.float,
    pdbx_average_fsc_free: Schema.float,
    pdbx_overall_ESU_B: Schema.float,
    pdbx_overall_ESU_ML: Schema.float,
};
export type Refine_Schema = typeof Refine_Schema;
export const Refine: Category<Refine_Schema> = {
    name: 'refine',
    schema: Refine_Schema,
};
